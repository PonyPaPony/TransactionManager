```markdown
# 💰 Personal Transaction Manager

Консольное приложение для учета личных финансов. Позволяет удобно записывать доходы и расходы, просматривать историю транзакций и анализировать бюджет.

## 📋 Функционал

*   **Учет операций:** Добавление доходов и расходов с указанием суммы, категории, даты и комментария.
*   **История:** Просмотр списка всех транзакций (сортировка по дате).
*   **Статистика:**
    *   Расчет общего баланса.
    *   Подсчет суммарных доходов и расходов.
    *   Детализация трат по категориям.
*   **Фильтрация:** (В разработке) Возможность просмотра данных за сегодня, неделю или месяц.
*   **Хранение данных:** Все записи сохраняются в JSON-файл, данные не теряются при перезапуске.

## 🛠 Технологии

*   **Язык:** Python 3.12+
*   **Формат данных:** JSON
*   **Архитектура:** Модульная (разделение на слои: Models, Services, UI).

## 🚀 Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/PonyPaPony/TransactionManager.git
    cd TransactionManager
    ```

2.  **Запустите приложение:**
    ```bash
    python main.py
    ```
    *(Дополнительные библиотеки устанавливать не нужно, используются только стандартные модули Python)*

## 📂 Структура проекта

```text
finance-manager/
├── data/                   # Папка для хранения файла с данными
├── models/
│   └── transaction.py      # Класс транзакции (данные)
├── services/
│   ├── file_manager.py     # Работа с чтением/записью файлов
│   └── transaction_manager.py # Основная бизнес-логика
├── utils/
│   └── validators.py       # Проверка корректности ввода
└── main.py                 # Точка входа и консольное меню
```

## 📝 Пример использования

1.  При запуске выберите пункт **"1. Добавить доход"**.
2.  Введите сумму (например, `50000`), категорию (например, `Зарплата`) и дату.
3.  Затем выберите пункт **"4. Расчет дохода/расхода"**, чтобы увидеть текущий баланс.
4.  Все данные сохранятся в `data/transactions.json`.
```
